---
- name: Run ctorgalson.macosbox role on macOS infrastructure.
  hosts: all
  gather_facts: true
  vars:
    prefs_prefs_directory: "{{ playbook_dir }}/files"
  roles:
    - role: ansible-role-macos-prefs
